{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://happiness-vibe.app/schemas/media.json",
  "title": "Media Entry",
  "description": "A media consumption tracking entry with date, type, and duration",
  "type": "object",
  "properties": {
    "date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "format": "date",
      "description": "Date in YYYY-MM-DD format",
      "examples": ["2024-10-23", "2023-12-25"]
    },
    "type": {
      "type": "string",
      "enum": ["book", "video", "podcast", "music"],
      "description": "Type of media consumed. Extensible - add new types to enum list as needed.",
      "examples": ["book", "video", "podcast", "music"]
    },
    "duration": {
      "type": "integer",
      "minimum": 1,
      "description": "Duration in minutes (positive integer)",
      "examples": [30, 60, 120, 240]
    }
  },
  "required": ["date", "type", "duration"],
  "additionalProperties": false
}
